overwrite: true
schema: "http://localhost:8000/___graphql"
documents:
  - "src/**/*.{ts,tsx}"
  - "src/WrapWithProvider.tsx"
generates:
  src/types/gql.d.ts:
    hooks:
      afterOneFileWrite:
        - prettier --write
    plugins:
      - "typescript"
      - "typescript-operations"
