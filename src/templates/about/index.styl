.about{
  .title {
    font-size: 1.25em
    color: #333
  }
  .content{
    :global {
      @import "../post/post.styl"

      .gatsby-resp-image-link {
        border-radius: 4px
        width 104%
        margin-left -2%
        overflow hidden
        cursor zoom-in
      }

      .gatsby-resp-image-wrapper {
        width 100%
        pointer-events none
      }
    }
  }
}
